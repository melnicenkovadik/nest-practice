import React from 'react';
import A from "../components/A";

const Error = (props) => (
    <>
        <h1>Станица не найдена</h1>
        <A href={"/"} text={'Вернуться    на  главную'}/>
    </>
);

export default Error;
